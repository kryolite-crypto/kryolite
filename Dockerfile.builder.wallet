ARG GITHUB_REPOSITORY
FROM ghcr.io/${GITHUB_REPOSITORY}/builder:base

COPY shared shared
COPY node node

COPY wallet wallet

ARG RUNTIME
RUN dotnet publish wallet \
  -c Release \
  -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true \
  --self-contained --runtime="$RUNTIME" \
  -o /build \
  && [ "$RUNTIME" != "win-x64" ] && mv /build/wallet /usr/local/bin/kryolite-wallet || true \
  && rm -rf /build

WORKDIR /kryolite
ENTRYPOINT [ "/usr/local/bin/kryolite-wallet" ]
